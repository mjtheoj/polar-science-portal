import { ComingSoon } from "@/components/layout/coming-soon";

export default function RepositoryPage() {
  return (
    <ComingSoon
      title="Digital Knowledge Repository"
      phase="Arriving in Phase 3"
      description="Expedition reports, publications, datasets and media, each with full metadata and an approval status, will be browsable and filterable here."
    />
  );
}
